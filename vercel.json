{
  "installCommand": "npm install && npm --prefix client install --include=dev && npm --prefix server install --include=dev",
  "buildCommand": "npm --prefix client run build",
  "outputDirectory": "client/dist"
}
